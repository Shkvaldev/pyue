var __index = {"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"index.html","title":"Pyue","text":"<p>Simple component-based Web UI generator in Python (inspired by Vue JS and Flutter)</p>"},{"location":"index.html#code-example","title":"Code example","text":"<pre><code>from pyue import Pyue, BackendType, Page\nfrom pyue.components import Stack, H1, H4, P, UL, LI\nfrom flask import Flask\n\n# Component data\nfeatures = [\n    \"\ud83c\udfaf Easy to use components\",\n    \"\u26a1 Tailwind included\",\n    \"\ud83d\ude80 Zero runtime overhead\",\n    \"\ud83d\udd0c Pluggable structure\",\n]\n\n# Component definition\ndef Feature(text):\n    return LI(\n        extra_classes=[\"p-3\", \"m-1\", \"border\", \"border-black\", \"rounded-xl\"],\n        content=[text],\n    )\n\n# Page definition\np = Page(\n    title=\"Hello Pyue!\",\n    content=[\n        Stack(\n            spacing=4,\n            align=\"items-center\",\n            content=[\n                H1(content=[\"Welcome!\"], extra_classes=[\"mt-3\"]),\n                P(content=[\"This page was built with Python \ud83d\udc0d\"]),\n                H4(extra_classes=[\"mt-3\", \"mb-3\"], content=[\"Why Pyue?\"]),\n                UL(\n                    extra_classes=[\"mt-1\"],\n                    content=[Feature(feature) for feature in features],\n                ),\n            ],\n        )\n    ],\n)\n\n# Creating Pyue app\napp = Pyue(BackendType.Flask)\n\n# Registering page\napp.add_page(page=p, url=\"/\")\n\nf = Flask(__name__)\napp.mount(f)\n\nf.run()\n</code></pre>"},{"location":"index.html#usage","title":"Usage","text":"<p>Follow docs link: https://shkvaldev.github.io/pyue</p>"},{"location":"index.html#goals","title":"Goals","text":"<ul> <li>Making MVP web apps creation easy in Python</li> <li>Component-based approach for building web UI without garbage from js frameworks</li> </ul>"},{"location":"index.html#features","title":"Features","text":"<ul> <li>Generating frontend from Python code that is fully integrated with backend (Flask, FastAPI)</li> <li>Very flexible (UI is processed in a separate router, simply includes onto your app)</li> <li>Quite extendable - you can easily implement your own components</li> </ul>"},{"location":"index.html#todo","title":"TODO","text":"<ul> <li><code>ResourceManager</code> for better resources handling</li> <li><code>HTMX</code> or <code>Alpine.js</code> integration for interactivity</li> <li>Custom HTML/CSS/JS embeddings (now available via content string)</li> <li><code>FastAPIBackend</code> for <code>FastAPI</code> support</li> <li>MORE COMPONENTS TO THE GOD OF COMPONENTS</li> <li>Experimental <code>Python-Js</code> translator</li> </ul>"},{"location":"advanced_usage.html","title":"Advanced usage","text":""},{"location":"advanced_usage.html#page-context-path-query-user-parameters","title":"Page context (path, query, user parameters)","text":"<p>When you use default <code>Pyue.add_page(Page, url)</code>, it looses ability to handle query parameters or some context (like DB info). But you can also provide special function, that handles context (which is called <code>context_func</code>): <pre><code># Path parameter example\npage = Page(\"greeting.html\")\napp.add_page(\n    page,\n    url=\"/hello/&lt;name&gt;\",\n    context_func=lambda name: {\"name\": name}\n)\n\n# Multiple path parameters\ndef product_context(category, product_id):\n    product = get_product(category, product_id)\n    related = get_related_products(category, limit=5)\n    return {\"product\": product, \"related\": related}\n\npage = Page(\"product.html\")\napp.add_page(page, url=\"/products/&lt;category&gt;/&lt;int:product_id&gt;\", context_func=product_context)\n\n# DB context example\ndef user_context(user_id):\n    user = get_user_by_id(user_id)\n    if user is None:\n        abort(404)\n    return {\"user\": user}\n\npage = Page(\"profile.html\")\napp.add_page(page, url=\"/user/&lt;int:user_id&gt;\", context_func=user_context)\n\n# Query parameters example\nfrom flask import request\n\ndef search_context():\n    query = request.args.get(\"q\", \"\")\n    page_num = request.args.get(\"page\", 1, type=int)\n    results = perform_search(query, page=page_num)\n    return {\"query\": query, \"results\": results, \"page\": page_num}\n\npage = Page(\"search.html\")\napp.add_page(page, url=\"/search\", context_func=search_context)\n</code></pre></p>"},{"location":"api_reference.html","title":"API Reference","text":"<p>In development</p> <p>There is only initial API reference, thats why the provided info may be not full</p>"},{"location":"api_reference.html#pyue","title":"Pyue","text":"<p>Master of WEB UI</p> Source code in <code>pyue/core/app.py</code> <pre><code>class Pyue:\n    \"\"\"Master of WEB UI\"\"\"\n\n    def __init__(\n        self,\n        backend_type: BackendType,\n        static_path: str | None = \"static\",\n        template_path: str | None = \"templates\",\n        logger=logger,\n    ) -&gt; None:\n        \"\"\"\n        Initialize the Pyue application.\n\n        Args:\n            backend_type (BackendType): The type of backend to use (e.g., Flask).\n            static_path (str | None, optional): Path to the static files directory.\n                Defaults to \"static\".\n            template_path (str | None, optional): Path to the templates directory.\n                Defaults to \"templates\".\n            logger: Logger instance for logging messages. Defaults to the loguru logger.\n\n        Raises:\n            UnsupportedBackendError: If the provided backend_type is not supported.\n        \"\"\"\n        self.logger = logger\n\n        # Creating backend\n        if not isinstance(backend_type, BackendType):\n            self.logger.error(\n                f\"Failed to create app with backend type `{backend_type}`\"\n            )\n            raise UnsupportedBackendError(backend_type)\n\n        if backend_type == BackendType.Flask:\n            self._backend = FlaskBackend(\n                static_path=static_path, template_path=template_path, logger=self.logger\n            )\n        elif backend_type == BackendType.FastAPI:\n            raise NotImplementedError()\n\n        self.static_path = static_path\n        self.template_path = template_path\n\n    @property\n    def router(self) -&gt; Any:\n        \"\"\"\n        Returns the framework-specific router object.\n\n        For Flask backend, this is a Blueprint. For other backends, it will be the\n        corresponding router object (e.g., APIRouter for FastAPI).\n\n        Returns:\n            Any: The router object from the underlying backend.\n        \"\"\"\n        return self._backend.router\n\n    def add_page(self, page: Page, url: str, **kwargs) -&gt; None:\n        \"\"\"\n        Add a page to the application and write its template file.\n\n        This method registers the page with the backend's router and writes the\n        page's content to a file in the template directory.\n\n        Args:\n            page (Page): The page object containing the template content and filename.\n            url (str): The URL rule for the page (e.g., '/' or '/about').\n            **kwargs: Additional keyword arguments passed to the backend's add_page method,\n                such as context_func.\n        \"\"\"\n        self._backend.add_page(page=page, url=url, **kwargs)\n        page.to_file(os.path.join(self.template_path, page.filename))\n        self.logger.debug(\n            f\"Page `{page.filename}` ({page.title[:50]}) successfully built\"\n        )\n\n    def mount(self, app, **kwargs):\n        \"\"\"\n        Mount the backend's router onto the given framework application.\n\n        This method delegates to the underlying backend's mount method to integrate\n        the router with the main application instance (e.g., a Flask app).\n\n        Args:\n            app: The framework application instance (e.g., Flask, FastAPI).\n            **kwargs: Additional keyword arguments passed to the backend's mount method.\n        \"\"\"\n        self._backend.mount(app, **kwargs)\n</code></pre>"},{"location":"api_reference.html#pyue.Pyue.router","title":"<code>router</code>  <code>property</code>","text":"<p>Returns the framework-specific router object.</p> <p>For Flask backend, this is a Blueprint. For other backends, it will be the corresponding router object (e.g., APIRouter for FastAPI).</p> <p>Returns:</p> Name Type Description <code>Any</code> <code>Any</code> <p>The router object from the underlying backend.</p>"},{"location":"api_reference.html#pyue.Pyue.__init__","title":"<code>__init__(backend_type, static_path='static', template_path='templates', logger=logger)</code>","text":"<p>Initialize the Pyue application.</p> <p>Parameters:</p> Name Type Description Default <code>backend_type</code> <code>BackendType</code> <p>The type of backend to use (e.g., Flask).</p> required <code>static_path</code> <code>str | None</code> <p>Path to the static files directory. Defaults to \"static\".</p> <code>'static'</code> <code>template_path</code> <code>str | None</code> <p>Path to the templates directory. Defaults to \"templates\".</p> <code>'templates'</code> <code>logger</code> <p>Logger instance for logging messages. Defaults to the loguru logger.</p> <code>logger</code> <p>Raises:</p> Type Description <code>UnsupportedBackendError</code> <p>If the provided backend_type is not supported.</p> Source code in <code>pyue/core/app.py</code> <pre><code>def __init__(\n    self,\n    backend_type: BackendType,\n    static_path: str | None = \"static\",\n    template_path: str | None = \"templates\",\n    logger=logger,\n) -&gt; None:\n    \"\"\"\n    Initialize the Pyue application.\n\n    Args:\n        backend_type (BackendType): The type of backend to use (e.g., Flask).\n        static_path (str | None, optional): Path to the static files directory.\n            Defaults to \"static\".\n        template_path (str | None, optional): Path to the templates directory.\n            Defaults to \"templates\".\n        logger: Logger instance for logging messages. Defaults to the loguru logger.\n\n    Raises:\n        UnsupportedBackendError: If the provided backend_type is not supported.\n    \"\"\"\n    self.logger = logger\n\n    # Creating backend\n    if not isinstance(backend_type, BackendType):\n        self.logger.error(\n            f\"Failed to create app with backend type `{backend_type}`\"\n        )\n        raise UnsupportedBackendError(backend_type)\n\n    if backend_type == BackendType.Flask:\n        self._backend = FlaskBackend(\n            static_path=static_path, template_path=template_path, logger=self.logger\n        )\n    elif backend_type == BackendType.FastAPI:\n        raise NotImplementedError()\n\n    self.static_path = static_path\n    self.template_path = template_path\n</code></pre>"},{"location":"api_reference.html#pyue.Pyue.add_page","title":"<code>add_page(page, url, **kwargs)</code>","text":"<p>Add a page to the application and write its template file.</p> <p>This method registers the page with the backend's router and writes the page's content to a file in the template directory.</p> <p>Parameters:</p> Name Type Description Default <code>page</code> <code>Page</code> <p>The page object containing the template content and filename.</p> required <code>url</code> <code>str</code> <p>The URL rule for the page (e.g., '/' or '/about').</p> required <code>**kwargs</code> <p>Additional keyword arguments passed to the backend's add_page method, such as context_func.</p> <code>{}</code> Source code in <code>pyue/core/app.py</code> <pre><code>def add_page(self, page: Page, url: str, **kwargs) -&gt; None:\n    \"\"\"\n    Add a page to the application and write its template file.\n\n    This method registers the page with the backend's router and writes the\n    page's content to a file in the template directory.\n\n    Args:\n        page (Page): The page object containing the template content and filename.\n        url (str): The URL rule for the page (e.g., '/' or '/about').\n        **kwargs: Additional keyword arguments passed to the backend's add_page method,\n            such as context_func.\n    \"\"\"\n    self._backend.add_page(page=page, url=url, **kwargs)\n    page.to_file(os.path.join(self.template_path, page.filename))\n    self.logger.debug(\n        f\"Page `{page.filename}` ({page.title[:50]}) successfully built\"\n    )\n</code></pre>"},{"location":"api_reference.html#pyue.Pyue.mount","title":"<code>mount(app, **kwargs)</code>","text":"<p>Mount the backend's router onto the given framework application.</p> <p>This method delegates to the underlying backend's mount method to integrate the router with the main application instance (e.g., a Flask app).</p> <p>Parameters:</p> Name Type Description Default <code>app</code> <p>The framework application instance (e.g., Flask, FastAPI).</p> required <code>**kwargs</code> <p>Additional keyword arguments passed to the backend's mount method.</p> <code>{}</code> Source code in <code>pyue/core/app.py</code> <pre><code>def mount(self, app, **kwargs):\n    \"\"\"\n    Mount the backend's router onto the given framework application.\n\n    This method delegates to the underlying backend's mount method to integrate\n    the router with the main application instance (e.g., a Flask app).\n\n    Args:\n        app: The framework application instance (e.g., Flask, FastAPI).\n        **kwargs: Additional keyword arguments passed to the backend's mount method.\n    \"\"\"\n    self._backend.mount(app, **kwargs)\n</code></pre>"},{"location":"api_reference.html#backend-api","title":"Backend (API)","text":"<p>               Bases: <code>ABC</code></p> <p>Abstract base class for HTTP server backends.</p> <p>This class defines the interface that all backend implementations must follow.</p> Source code in <code>pyue/core/backend.py</code> <pre><code>class Backend(ABC):\n    \"\"\"Abstract base class for HTTP server backends.\n\n    This class defines the interface that all backend implementations\n    must follow.\n    \"\"\"\n\n    @property\n    def router(self) -&gt; Any:\n        \"\"\"\n        Abstract property that returns the framework-specific router object.\n\n        The router contains all registered frontend endpoints. For Flask,\n        this is a Blueprint; for FastAPI, it would be an APIRouter.\n\n        Returns:\n            Any: The router instance containing the frontend routes.\n        \"\"\"\n        raise NotImplementedError()\n\n    @abstractmethod\n    def add_page(self, page: Page, url: str, **kwargs) -&gt; None:\n        \"\"\"\n        Add a page to the backend's router.\n\n        This method registers a new route for the given URL and associates it\n        with the provided page. The page's content will be served when the URL\n        is accessed.\n\n        Args:\n            page (Page): The page object containing template and content information.\n            url (str): The URL path at which the page should be served.\n            **kwargs: Additional framework-specific keyword arguments or context function.\n        \"\"\"\n        raise NotImplementedError()\n\n    @abstractmethod\n    def mount(self, app, **kwargs) -&gt; None:\n        \"\"\"\n        Mount the backend's router onto the main application.\n\n        This method integrates the router containing all frontend pages into\n        the main application instance (e.g., a Flask or FastAPI app).\n\n        Args:\n            app: The main framework application instance.\n            **kwargs: Additional framework-specific keyword arguments for mounting.\n        \"\"\"\n        raise NotImplementedError()\n</code></pre>"},{"location":"api_reference.html#pyue.Backend.router","title":"<code>router</code>  <code>property</code>","text":"<p>Abstract property that returns the framework-specific router object.</p> <p>The router contains all registered frontend endpoints. For Flask, this is a Blueprint; for FastAPI, it would be an APIRouter.</p> <p>Returns:</p> Name Type Description <code>Any</code> <code>Any</code> <p>The router instance containing the frontend routes.</p>"},{"location":"api_reference.html#pyue.Backend.add_page","title":"<code>add_page(page, url, **kwargs)</code>  <code>abstractmethod</code>","text":"<p>Add a page to the backend's router.</p> <p>This method registers a new route for the given URL and associates it with the provided page. The page's content will be served when the URL is accessed.</p> <p>Parameters:</p> Name Type Description Default <code>page</code> <code>Page</code> <p>The page object containing template and content information.</p> required <code>url</code> <code>str</code> <p>The URL path at which the page should be served.</p> required <code>**kwargs</code> <p>Additional framework-specific keyword arguments or context function.</p> <code>{}</code> Source code in <code>pyue/core/backend.py</code> <pre><code>@abstractmethod\ndef add_page(self, page: Page, url: str, **kwargs) -&gt; None:\n    \"\"\"\n    Add a page to the backend's router.\n\n    This method registers a new route for the given URL and associates it\n    with the provided page. The page's content will be served when the URL\n    is accessed.\n\n    Args:\n        page (Page): The page object containing template and content information.\n        url (str): The URL path at which the page should be served.\n        **kwargs: Additional framework-specific keyword arguments or context function.\n    \"\"\"\n    raise NotImplementedError()\n</code></pre>"},{"location":"api_reference.html#pyue.Backend.mount","title":"<code>mount(app, **kwargs)</code>  <code>abstractmethod</code>","text":"<p>Mount the backend's router onto the main application.</p> <p>This method integrates the router containing all frontend pages into the main application instance (e.g., a Flask or FastAPI app).</p> <p>Parameters:</p> Name Type Description Default <code>app</code> <p>The main framework application instance.</p> required <code>**kwargs</code> <p>Additional framework-specific keyword arguments for mounting.</p> <code>{}</code> Source code in <code>pyue/core/backend.py</code> <pre><code>@abstractmethod\ndef mount(self, app, **kwargs) -&gt; None:\n    \"\"\"\n    Mount the backend's router onto the main application.\n\n    This method integrates the router containing all frontend pages into\n    the main application instance (e.g., a Flask or FastAPI app).\n\n    Args:\n        app: The main framework application instance.\n        **kwargs: Additional framework-specific keyword arguments for mounting.\n    \"\"\"\n    raise NotImplementedError()\n</code></pre>"},{"location":"api_reference.html#backendtype","title":"BackendType","text":"<p>               Bases: <code>Enum</code></p> <p>Available backend frameworks</p> Source code in <code>pyue/core/backend.py</code> <pre><code>class BackendType(Enum):\n    \"\"\"Available backend frameworks\"\"\"\n\n    Flask = 1\n    FastAPI = 2\n</code></pre>"},{"location":"api_reference.html#flaskbackend","title":"FlaskBackend","text":"<p>               Bases: <code>Backend</code></p> <p>Backend for Flask web framework</p> Source code in <code>pyue/backend/flask.py</code> <pre><code>class FlaskBackend(Backend):\n    \"\"\"\n    Backend for Flask web framework\n    \"\"\"\n\n    def __init__(\n        self,\n        static_path: str,\n        template_path: str,\n        router: Blueprint | None = None,\n        logger=logger,\n    ) -&gt; None:\n        \"\"\"\n        Initialize the FlaskBackend.\n\n        Args:\n            static_path (str): Path to the static files directory.\n            template_path (str): Path to the templates directory.\n            router (Blueprint | None, optional): An existing Flask Blueprint to use.\n                If None, a new Blueprint is created. Defaults to None.\n            logger: Logger instance for logging messages. Defaults to the loguru logger.\n        \"\"\"\n        super().__init__()\n        self.logger = logger\n        if router and isinstance(router, Blueprint):\n            self._router = router\n        else:\n            self._router = Blueprint(\n                \"frontend\",\n                __name__,\n                template_folder=template_path,\n                static_folder=static_path,\n            )\n        self.static_path = static_path\n        self.template_path = template_path\n\n    @property\n    def router(self) -&gt; Blueprint:\n        \"\"\"\n        Blueprint: The Flask Blueprint associated with this backend.\n        \"\"\"\n        return self._router\n\n    def add_page(self, page: Page, url: str, context_func=None) -&gt; None:\n        \"\"\"\n        Add a page to the backend's Blueprint.\n\n        Args:\n            page (Page): The page object containing the template filename and other page data.\n            url (str): The URL rule for the page (e.g., '/' or '/about').\n            context_func (callable, optional): A function that returns a dictionary of additional context variables.\n                The function receives any keyword arguments captured from the URL and should return a dict.\n                Defaults to None.\n\n        Raises:\n            AddPageError: If adding the page to the Blueprint fails due to an exception.\n        \"\"\"\n        try:\n            # Handler with context support\n            def handler(**kwargs):\n                try:\n                    context = kwargs\n                    if context_func:\n                        extra_context = context_func(**kwargs)\n                        if isinstance(extra_context, dict):\n                            context.update(extra_context)\n                    return render_template(page.filename, **context)\n                except Exception:\n                    self.logger.error(f\"Failed to show page: {traceback.format_exc()}\")\n                    abort(500)\n\n            self._router.add_url_rule(\n                url, endpoint=url.replace(\"/\", \"_\"), view_func=handler, methods=[\"GET\"]\n            )\n        except Exception as e:\n            self.logger.error(f\"Failed to add page: {e}\")\n            raise AddPageError(traceback.format_exc())\n\n    def mount(self, app: Flask) -&gt; None:\n        \"\"\"\n        Mount the backend's Blueprint onto a Flask application.\n\n        Args:\n            app (Flask): The Flask application instance.\n\n        Raises:\n            MountBackendError: If registering the blueprint with the Flask app fails.\n        \"\"\"\n        try:\n            app.template_folder = self.template_path\n            app.static_folder = self.static_path\n            app.register_blueprint(self.router)\n        except Exception as e:\n            self.logger.error(f\"Failed to mount backend: {e}\")\n            raise MountBackendError(traceback.format_exc())\n</code></pre>"},{"location":"api_reference.html#pyue.backend.flask.FlaskBackend.router","title":"<code>router</code>  <code>property</code>","text":"<p>Blueprint: The Flask Blueprint associated with this backend.</p>"},{"location":"api_reference.html#pyue.backend.flask.FlaskBackend.__init__","title":"<code>__init__(static_path, template_path, router=None, logger=logger)</code>","text":"<p>Initialize the FlaskBackend.</p> <p>Parameters:</p> Name Type Description Default <code>static_path</code> <code>str</code> <p>Path to the static files directory.</p> required <code>template_path</code> <code>str</code> <p>Path to the templates directory.</p> required <code>router</code> <code>Blueprint | None</code> <p>An existing Flask Blueprint to use. If None, a new Blueprint is created. Defaults to None.</p> <code>None</code> <code>logger</code> <p>Logger instance for logging messages. Defaults to the loguru logger.</p> <code>logger</code> Source code in <code>pyue/backend/flask.py</code> <pre><code>def __init__(\n    self,\n    static_path: str,\n    template_path: str,\n    router: Blueprint | None = None,\n    logger=logger,\n) -&gt; None:\n    \"\"\"\n    Initialize the FlaskBackend.\n\n    Args:\n        static_path (str): Path to the static files directory.\n        template_path (str): Path to the templates directory.\n        router (Blueprint | None, optional): An existing Flask Blueprint to use.\n            If None, a new Blueprint is created. Defaults to None.\n        logger: Logger instance for logging messages. Defaults to the loguru logger.\n    \"\"\"\n    super().__init__()\n    self.logger = logger\n    if router and isinstance(router, Blueprint):\n        self._router = router\n    else:\n        self._router = Blueprint(\n            \"frontend\",\n            __name__,\n            template_folder=template_path,\n            static_folder=static_path,\n        )\n    self.static_path = static_path\n    self.template_path = template_path\n</code></pre>"},{"location":"api_reference.html#pyue.backend.flask.FlaskBackend.add_page","title":"<code>add_page(page, url, context_func=None)</code>","text":"<p>Add a page to the backend's Blueprint.</p> <p>Parameters:</p> Name Type Description Default <code>page</code> <code>Page</code> <p>The page object containing the template filename and other page data.</p> required <code>url</code> <code>str</code> <p>The URL rule for the page (e.g., '/' or '/about').</p> required <code>context_func</code> <code>callable</code> <p>A function that returns a dictionary of additional context variables. The function receives any keyword arguments captured from the URL and should return a dict. Defaults to None.</p> <code>None</code> <p>Raises:</p> Type Description <code>AddPageError</code> <p>If adding the page to the Blueprint fails due to an exception.</p> Source code in <code>pyue/backend/flask.py</code> <pre><code>def add_page(self, page: Page, url: str, context_func=None) -&gt; None:\n    \"\"\"\n    Add a page to the backend's Blueprint.\n\n    Args:\n        page (Page): The page object containing the template filename and other page data.\n        url (str): The URL rule for the page (e.g., '/' or '/about').\n        context_func (callable, optional): A function that returns a dictionary of additional context variables.\n            The function receives any keyword arguments captured from the URL and should return a dict.\n            Defaults to None.\n\n    Raises:\n        AddPageError: If adding the page to the Blueprint fails due to an exception.\n    \"\"\"\n    try:\n        # Handler with context support\n        def handler(**kwargs):\n            try:\n                context = kwargs\n                if context_func:\n                    extra_context = context_func(**kwargs)\n                    if isinstance(extra_context, dict):\n                        context.update(extra_context)\n                return render_template(page.filename, **context)\n            except Exception:\n                self.logger.error(f\"Failed to show page: {traceback.format_exc()}\")\n                abort(500)\n\n        self._router.add_url_rule(\n            url, endpoint=url.replace(\"/\", \"_\"), view_func=handler, methods=[\"GET\"]\n        )\n    except Exception as e:\n        self.logger.error(f\"Failed to add page: {e}\")\n        raise AddPageError(traceback.format_exc())\n</code></pre>"},{"location":"api_reference.html#pyue.backend.flask.FlaskBackend.mount","title":"<code>mount(app)</code>","text":"<p>Mount the backend's Blueprint onto a Flask application.</p> <p>Parameters:</p> Name Type Description Default <code>app</code> <code>Flask</code> <p>The Flask application instance.</p> required <p>Raises:</p> Type Description <code>MountBackendError</code> <p>If registering the blueprint with the Flask app fails.</p> Source code in <code>pyue/backend/flask.py</code> <pre><code>def mount(self, app: Flask) -&gt; None:\n    \"\"\"\n    Mount the backend's Blueprint onto a Flask application.\n\n    Args:\n        app (Flask): The Flask application instance.\n\n    Raises:\n        MountBackendError: If registering the blueprint with the Flask app fails.\n    \"\"\"\n    try:\n        app.template_folder = self.template_path\n        app.static_folder = self.static_path\n        app.register_blueprint(self.router)\n    except Exception as e:\n        self.logger.error(f\"Failed to mount backend: {e}\")\n        raise MountBackendError(traceback.format_exc())\n</code></pre>"},{"location":"api_reference.html#page","title":"Page","text":"<p>Represents WEB page (root for all components)</p> Source code in <code>pyue/core/page.py</code> <pre><code>class Page:\n    \"\"\"Represents WEB page (root for all components)\"\"\"\n\n    def __init__(\n        self,\n        title: str,\n        lang: str = \"en\",\n        favicon: str | None = None,\n        content: Optional[List[Union[Component, str]]] = None,\n        filename: str | None = None,\n        static_path: str = \"static\",\n        requirements: set[Resource] | None = None,\n    ) -&gt; None:\n        \"\"\"\n        Initialize a new Page instance.\n\n        Args:\n            title (str): The title of the page, displayed in the browser tab.\n            lang (str, optional): The language code for the page (e.g., 'en', 'ru').\n                Defaults to \"en\".\n            favicon (str | None, optional): Path or URL to the favicon. Defaults to None.\n            content: List of component instances or strings to nest inside this page.\n            filename (str | None, optional): Desired filename for the generated HTML file.\n                If None, a unique filename is generated from the title. Defaults to None.\n            static_path (str | None, optional): Path to the static files directory.\n                Defaults to \"static\".\n        \"\"\"\n        self.title = title\n        self.lang = lang\n        self.favicon = favicon or \"favicon.ico\"\n        self.filename = filename\n        if not self.filename:\n            hash_object = hashlib.sha256(title.encode())\n            hash_hex = hash_object.hexdigest()[:8]\n            self.filename = f\"page_{hash_hex}.html\"\n        self.static_path = static_path\n        self.content = content or []\n\n        TAILWIND_CSS = ResourceCss(\n            \"https://cdn.jsdelivr.net/npm/tailwindcss@2/dist/tailwind.min.css\"\n        )\n        self.requirements = requirements or {TAILWIND_CSS}\n\n    def to_string(self) -&gt; str:\n        \"\"\"\n        Render the page to an HTML string.\n\n        The method generates HTML template using page data\n        (title, language, requirements, and content), and returns the resulting string.\n\n        Returns:\n            str: The fully rendered HTML content of the page.\n\n        Raises:\n            PageBuildingError: If the template file cannot be read or any other\n                rendering error occurs.\n        \"\"\"\n        # Using html template\n        template_path = os.path.join(__root__, \"snippets\", \"page.html\")\n        try:\n            with open(template_path, \"r\") as f:\n                result = f.read()\n\n            result = result.replace(\"$TITLE$\", self.title)\n            result = result.replace(\"$LANG$\", self.lang)\n            result = result.replace(\"$FAVICON$\", self.static_path + \"/\" + self.favicon)\n\n            contents = []\n            for content in self.content:\n                if isinstance(content, str):\n                    contents.append(\" \" * 4 + content)\n                    continue\n                elif isinstance(content, Component):\n                    contents.extend(content.to_lines(level=1))\n            result = result.replace(\"$CONTENT$\", \"\\n\".join(contents))\n            reqs = set()\n            for req in self.requirements:\n                if isinstance(req, ResourceCss):\n                    # Include css link here\n                    req_file = os.path.basename(req.resource)\n                    reqs.add(\n                        f'&lt;link rel=\"stylesheet\" href=\"{self.static_path+'/'+req_file}\"&gt;'\n                    )\n                elif isinstance(req, ResourceJs):\n                    # Include js link here\n                    req_file = os.path.basename(req.resource)\n                    reqs.add(f'&lt;script src=\"{self.static_path+'/'+req_file}\"&gt;&lt;/script&gt;')\n            result = result.replace(\"$REQUIREMENTS$\", \"\\n\".join(reqs))\n            return result\n        except Exception:\n            raise PageBuildingError(traceback.format_exc())\n\n    def to_file(self, file_path: str | None) -&gt; None:\n        \"\"\"\n        Render the page and save it to a file.\n\n        This method writes the rendered HTML content to the specified file path.\n        If no path is provided, the page's filename attribute is used.\n\n        Args:\n            file_path (str | None): The full path where the HTML file should be saved.\n                If None, the page's internally generated filename is used.\n\n        Raises:\n            PageBuildingError: If writing to the file fails due to an I/O error\n                or a rendering error.\n        \"\"\"\n        try:\n            path = file_path or self.filename\n            with open(path, \"w\") as f:\n                f.write(self.to_string())\n        except Exception:\n            raise PageBuildingError(traceback.format_exc())\n\n        for requirement in self.requirements:\n            try:\n                req_path = os.path.join(\n                    self.static_path, os.path.basename(requirement.resource)\n                )\n                if os.path.exists(req_path):\n                    print(f\"[@] Found cached requirement `{req_path}`, skipping ...\")\n                    continue\n                print(f\"[*] Obtaining {requirement.resource} ...\")\n                print(\n                    f\"[*] Downloaded requirement `{download_file(requirement.resource, req_path)}`\"\n                )\n            except Exception:\n                raise ResourceDownloadError(requirement.resource)\n</code></pre>"},{"location":"api_reference.html#pyue.Page.__init__","title":"<code>__init__(title, lang='en', favicon=None, content=None, filename=None, static_path='static', requirements=None)</code>","text":"<p>Initialize a new Page instance.</p> <p>Parameters:</p> Name Type Description Default <code>title</code> <code>str</code> <p>The title of the page, displayed in the browser tab.</p> required <code>lang</code> <code>str</code> <p>The language code for the page (e.g., 'en', 'ru'). Defaults to \"en\".</p> <code>'en'</code> <code>favicon</code> <code>str | None</code> <p>Path or URL to the favicon. Defaults to None.</p> <code>None</code> <code>content</code> <code>Optional[List[Union[Component, str]]]</code> <p>List of component instances or strings to nest inside this page.</p> <code>None</code> <code>filename</code> <code>str | None</code> <p>Desired filename for the generated HTML file. If None, a unique filename is generated from the title. Defaults to None.</p> <code>None</code> <code>static_path</code> <code>str | None</code> <p>Path to the static files directory. Defaults to \"static\".</p> <code>'static'</code> Source code in <code>pyue/core/page.py</code> <pre><code>def __init__(\n    self,\n    title: str,\n    lang: str = \"en\",\n    favicon: str | None = None,\n    content: Optional[List[Union[Component, str]]] = None,\n    filename: str | None = None,\n    static_path: str = \"static\",\n    requirements: set[Resource] | None = None,\n) -&gt; None:\n    \"\"\"\n    Initialize a new Page instance.\n\n    Args:\n        title (str): The title of the page, displayed in the browser tab.\n        lang (str, optional): The language code for the page (e.g., 'en', 'ru').\n            Defaults to \"en\".\n        favicon (str | None, optional): Path or URL to the favicon. Defaults to None.\n        content: List of component instances or strings to nest inside this page.\n        filename (str | None, optional): Desired filename for the generated HTML file.\n            If None, a unique filename is generated from the title. Defaults to None.\n        static_path (str | None, optional): Path to the static files directory.\n            Defaults to \"static\".\n    \"\"\"\n    self.title = title\n    self.lang = lang\n    self.favicon = favicon or \"favicon.ico\"\n    self.filename = filename\n    if not self.filename:\n        hash_object = hashlib.sha256(title.encode())\n        hash_hex = hash_object.hexdigest()[:8]\n        self.filename = f\"page_{hash_hex}.html\"\n    self.static_path = static_path\n    self.content = content or []\n\n    TAILWIND_CSS = ResourceCss(\n        \"https://cdn.jsdelivr.net/npm/tailwindcss@2/dist/tailwind.min.css\"\n    )\n    self.requirements = requirements or {TAILWIND_CSS}\n</code></pre>"},{"location":"api_reference.html#pyue.Page.to_file","title":"<code>to_file(file_path)</code>","text":"<p>Render the page and save it to a file.</p> <p>This method writes the rendered HTML content to the specified file path. If no path is provided, the page's filename attribute is used.</p> <p>Parameters:</p> Name Type Description Default <code>file_path</code> <code>str | None</code> <p>The full path where the HTML file should be saved. If None, the page's internally generated filename is used.</p> required <p>Raises:</p> Type Description <code>PageBuildingError</code> <p>If writing to the file fails due to an I/O error or a rendering error.</p> Source code in <code>pyue/core/page.py</code> <pre><code>def to_file(self, file_path: str | None) -&gt; None:\n    \"\"\"\n    Render the page and save it to a file.\n\n    This method writes the rendered HTML content to the specified file path.\n    If no path is provided, the page's filename attribute is used.\n\n    Args:\n        file_path (str | None): The full path where the HTML file should be saved.\n            If None, the page's internally generated filename is used.\n\n    Raises:\n        PageBuildingError: If writing to the file fails due to an I/O error\n            or a rendering error.\n    \"\"\"\n    try:\n        path = file_path or self.filename\n        with open(path, \"w\") as f:\n            f.write(self.to_string())\n    except Exception:\n        raise PageBuildingError(traceback.format_exc())\n\n    for requirement in self.requirements:\n        try:\n            req_path = os.path.join(\n                self.static_path, os.path.basename(requirement.resource)\n            )\n            if os.path.exists(req_path):\n                print(f\"[@] Found cached requirement `{req_path}`, skipping ...\")\n                continue\n            print(f\"[*] Obtaining {requirement.resource} ...\")\n            print(\n                f\"[*] Downloaded requirement `{download_file(requirement.resource, req_path)}`\"\n            )\n        except Exception:\n            raise ResourceDownloadError(requirement.resource)\n</code></pre>"},{"location":"api_reference.html#pyue.Page.to_string","title":"<code>to_string()</code>","text":"<p>Render the page to an HTML string.</p> <p>The method generates HTML template using page data (title, language, requirements, and content), and returns the resulting string.</p> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>The fully rendered HTML content of the page.</p> <p>Raises:</p> Type Description <code>PageBuildingError</code> <p>If the template file cannot be read or any other rendering error occurs.</p> Source code in <code>pyue/core/page.py</code> <pre><code>def to_string(self) -&gt; str:\n    \"\"\"\n    Render the page to an HTML string.\n\n    The method generates HTML template using page data\n    (title, language, requirements, and content), and returns the resulting string.\n\n    Returns:\n        str: The fully rendered HTML content of the page.\n\n    Raises:\n        PageBuildingError: If the template file cannot be read or any other\n            rendering error occurs.\n    \"\"\"\n    # Using html template\n    template_path = os.path.join(__root__, \"snippets\", \"page.html\")\n    try:\n        with open(template_path, \"r\") as f:\n            result = f.read()\n\n        result = result.replace(\"$TITLE$\", self.title)\n        result = result.replace(\"$LANG$\", self.lang)\n        result = result.replace(\"$FAVICON$\", self.static_path + \"/\" + self.favicon)\n\n        contents = []\n        for content in self.content:\n            if isinstance(content, str):\n                contents.append(\" \" * 4 + content)\n                continue\n            elif isinstance(content, Component):\n                contents.extend(content.to_lines(level=1))\n        result = result.replace(\"$CONTENT$\", \"\\n\".join(contents))\n        reqs = set()\n        for req in self.requirements:\n            if isinstance(req, ResourceCss):\n                # Include css link here\n                req_file = os.path.basename(req.resource)\n                reqs.add(\n                    f'&lt;link rel=\"stylesheet\" href=\"{self.static_path+'/'+req_file}\"&gt;'\n                )\n            elif isinstance(req, ResourceJs):\n                # Include js link here\n                req_file = os.path.basename(req.resource)\n                reqs.add(f'&lt;script src=\"{self.static_path+'/'+req_file}\"&gt;&lt;/script&gt;')\n        result = result.replace(\"$REQUIREMENTS$\", \"\\n\".join(reqs))\n        return result\n    except Exception:\n        raise PageBuildingError(traceback.format_exc())\n</code></pre>"},{"location":"api_reference.html#component","title":"Component","text":"<p>Represents a reusable page component that can be rendered to HTML.</p> <p>Components are the building blocks of Pyue pages, allowing for modular and dynamic HTML generation with Jinja2 template support.</p> Source code in <code>pyue/core/component.py</code> <pre><code>class Component:\n    \"\"\"Represents a reusable page component that can be rendered to HTML.\n\n    Components are the building blocks of Pyue pages, allowing for modular\n    and dynamic HTML generation with Jinja2 template support.\n    \"\"\"\n\n    def __init__(\n        self,\n        tag: str = \"div\",\n        idx: str | None = None,\n        classes: list[str] | None = None,\n        extra_classes: list[str] | None = None,\n        v_content: str | None = None,\n        v_if: str | None = None,\n        v_for: str | None = None,\n        content: Optional[List[Union[\"Component\", str]]] = None,\n        no_closing: bool = False,\n        **kwargs,\n    ) -&gt; None:\n        \"\"\"Initialize a new Component instance.\n\n        Args:\n            tag: HTML tag name for the component (e.g., 'div', 'span', 'section').\n                Defaults to \"div\".\n            idx: Optional identifier that becomes the HTML id attribute.\n            classes: List of CSS classes to apply to the component.\n            extra_classes: Additional CSS classes appended to existing classes.\n                Useful for extending component styles without modifying base classes.\n            v_content: Variable name for Jinja2 content interpolation. The content\n                will be rendered as {{ variable_name }} in the template.\n            v_if: Jinja2 conditional expression string. The component renders only\n                if this expression evaluates to true. Example: \"user.is_authenticated\".\n            v_for: Jinja2 loop definition string. Enables repetitive rendering.\n                Example: \"item in items\" or \"key, value in dict.items()\".\n            content: List of child Component instances or strings to nest inside\n                this component. Children are rendered recursively.\n            no_closing: Flag indicating if the tag should be self-closing without\n                a closing tag. Use for void elements like &lt;img&gt;, &lt;br&gt;, &lt;hr&gt;.\n            **kwargs: Additional HTML attributes and CSS styles:\n                - CSS styles: Any keyword argument not prefixed with \"attr__\" is\n                  treated as a CSS style. Underscores in keys are converted to\n                  hyphens (e.g., font_size becomes font-size).\n                - HTML attributes: Keys prefixed with \"attr__\" are treated as\n                  raw HTML attributes (e.g., attr__data_id=\"123\" becomes data-id=\"123\").\n\n        Raises:\n            ComponentBuildingError: May be raised during rendering if template\n                syntax is invalid or required variables are missing.\n        \"\"\"\n        self.tag = tag\n        self.idx = idx\n        self.classes = classes or []\n        self.extra_classes = extra_classes or []\n        self.v_content = v_content\n        self.v_if = v_if\n        self.v_for = v_for\n        self.content = content or []\n        self.no_closing = no_closing\n        self.additional_attrs = {}\n        # Using other args as `style` data\n        self.styles = {}\n        for k, v in kwargs.items():\n            if not v:\n                continue\n            if k.startswith(\"attr__\"):\n                self.additional_attrs[k.split(\"attr__\")[-1]] = v\n                continue\n            self.styles[k.replace(\"_\", \"-\")] = v\n\n    def to_lines(self, level: int = 0) -&gt; list[str]:\n        \"\"\"Render the component to a list of HTML strings.\n\n        This method generates a list of the complete HTML representations of the component parts,\n        including all child components and applying any Jinja2 template directives\n        (v_if, v_for, v_content). The rendering process:\n        1. Processes the component's HTML tag and attributes\n        2. Recursively renders all child components\n        3. Applies Jinja2 template syntax for dynamic content\n        4. Combines everything into a final list of HTML strings\n\n        Returns:\n            List of fully rendered HTML content of the component as strings.\n\n        Raises:\n            ComponentBuildingError: If an error occurs during template strings\n                rendering, such as malformed template syntax or missing variables.\n\n        Example:\n            ```python\n            component = Component(tag=\"h1\", v_content=\"title\")\n            html = component.to_lines()\n            print(html)\n            # ['&lt;h1&gt;', '    {{ title }}', '\\n&lt;/h1&gt;']\n            ```\n        \"\"\"\n        try:\n            indent = \" \" * 4 * level\n            inner_indent = \" \" * 4 * (level + 1)\n            result = []\n\n            attrs = []\n            if self.idx:\n                attrs.append(f'id=\"{self.idx}\"')\n            if self.classes or self.extra_classes:\n                classes = self.classes + self.extra_classes\n                attrs.append(f'class=\"{\" \".join(classes)}\"')\n\n            if self.additional_attrs:\n                additional_attrs = []\n                for k, v in self.additional_attrs.items():\n                    additional_attrs.append(f'{k}=\"{v}\"')\n                attrs.extend(additional_attrs)\n\n            if self.styles:\n                styles = []\n                for k, v in self.styles.items():\n                    styles.append(f\"{k}:{v}\")\n\n                attrs.append(f'style=\"{\";\".join(styles)}\"')\n            attrs = \" \" + \" \".join(attrs) if attrs else \"\"\n\n            result.append(f\"{indent}&lt;{self.tag}{attrs}&gt;\")\n\n            if self.v_content:\n                result.append(f\"{inner_indent}{{{{ {self.v_content} }}}}\")\n\n            for content in self.content:\n                if isinstance(content, str):\n                    result.append(inner_indent + content)\n                    continue\n                elif isinstance(content, Component):\n                    result.extend(content.to_lines(level=level + 1))\n\n            # For handling tags like &lt;hr&gt;\n            if not self.no_closing:\n                result.append(f\"\\n{indent}&lt;/{self.tag}&gt;\")\n\n            if self.v_if:\n                result = (\n                    [f\"{indent}{{% if {self.v_if} %}}\"]\n                    + result\n                    + [f\"{indent}{{% endif %}}\"]\n                )\n            if self.v_for:\n                result = (\n                    [f\"{indent}{{% for {self.v_for} %}}\"]\n                    + result\n                    + [f\"{indent}{{% endfor %}}\"]\n                )\n\n            return result\n        except Exception:\n            raise ComponentBuildingError(traceback.format_exc())\n\n    def to_string(self, level: int = 0) -&gt; str:\n        \"\"\"Return the component as a single string.\"\"\"\n        return \"\\n\".join(self.to_lines(level))\n</code></pre>"},{"location":"api_reference.html#pyue.Component.__init__","title":"<code>__init__(tag='div', idx=None, classes=None, extra_classes=None, v_content=None, v_if=None, v_for=None, content=None, no_closing=False, **kwargs)</code>","text":"<p>Initialize a new Component instance.</p> <p>Parameters:</p> Name Type Description Default <code>tag</code> <code>str</code> <p>HTML tag name for the component (e.g., 'div', 'span', 'section'). Defaults to \"div\".</p> <code>'div'</code> <code>idx</code> <code>str | None</code> <p>Optional identifier that becomes the HTML id attribute.</p> <code>None</code> <code>classes</code> <code>list[str] | None</code> <p>List of CSS classes to apply to the component.</p> <code>None</code> <code>extra_classes</code> <code>list[str] | None</code> <p>Additional CSS classes appended to existing classes. Useful for extending component styles without modifying base classes.</p> <code>None</code> <code>v_content</code> <code>str | None</code> <p>Variable name for Jinja2 content interpolation. The content will be rendered as {{ variable_name }} in the template.</p> <code>None</code> <code>v_if</code> <code>str | None</code> <p>Jinja2 conditional expression string. The component renders only if this expression evaluates to true. Example: \"user.is_authenticated\".</p> <code>None</code> <code>v_for</code> <code>str | None</code> <p>Jinja2 loop definition string. Enables repetitive rendering. Example: \"item in items\" or \"key, value in dict.items()\".</p> <code>None</code> <code>content</code> <code>Optional[List[Union[Component, str]]]</code> <p>List of child Component instances or strings to nest inside this component. Children are rendered recursively.</p> <code>None</code> <code>no_closing</code> <code>bool</code> <p>Flag indicating if the tag should be self-closing without a closing tag. Use for void elements like , , .</p> <code>False</code> <code>**kwargs</code> <p>Additional HTML attributes and CSS styles: - CSS styles: Any keyword argument not prefixed with \"attr__\" is   treated as a CSS style. Underscores in keys are converted to   hyphens (e.g., font_size becomes font-size). - HTML attributes: Keys prefixed with \"attr__\" are treated as   raw HTML attributes (e.g., attr__data_id=\"123\" becomes data-id=\"123\").</p> <code>{}</code> <p>Raises:</p> Type Description <code>ComponentBuildingError</code> <p>May be raised during rendering if template syntax is invalid or required variables are missing.</p> Source code in <code>pyue/core/component.py</code> <pre><code>def __init__(\n    self,\n    tag: str = \"div\",\n    idx: str | None = None,\n    classes: list[str] | None = None,\n    extra_classes: list[str] | None = None,\n    v_content: str | None = None,\n    v_if: str | None = None,\n    v_for: str | None = None,\n    content: Optional[List[Union[\"Component\", str]]] = None,\n    no_closing: bool = False,\n    **kwargs,\n) -&gt; None:\n    \"\"\"Initialize a new Component instance.\n\n    Args:\n        tag: HTML tag name for the component (e.g., 'div', 'span', 'section').\n            Defaults to \"div\".\n        idx: Optional identifier that becomes the HTML id attribute.\n        classes: List of CSS classes to apply to the component.\n        extra_classes: Additional CSS classes appended to existing classes.\n            Useful for extending component styles without modifying base classes.\n        v_content: Variable name for Jinja2 content interpolation. The content\n            will be rendered as {{ variable_name }} in the template.\n        v_if: Jinja2 conditional expression string. The component renders only\n            if this expression evaluates to true. Example: \"user.is_authenticated\".\n        v_for: Jinja2 loop definition string. Enables repetitive rendering.\n            Example: \"item in items\" or \"key, value in dict.items()\".\n        content: List of child Component instances or strings to nest inside\n            this component. Children are rendered recursively.\n        no_closing: Flag indicating if the tag should be self-closing without\n            a closing tag. Use for void elements like &lt;img&gt;, &lt;br&gt;, &lt;hr&gt;.\n        **kwargs: Additional HTML attributes and CSS styles:\n            - CSS styles: Any keyword argument not prefixed with \"attr__\" is\n              treated as a CSS style. Underscores in keys are converted to\n              hyphens (e.g., font_size becomes font-size).\n            - HTML attributes: Keys prefixed with \"attr__\" are treated as\n              raw HTML attributes (e.g., attr__data_id=\"123\" becomes data-id=\"123\").\n\n    Raises:\n        ComponentBuildingError: May be raised during rendering if template\n            syntax is invalid or required variables are missing.\n    \"\"\"\n    self.tag = tag\n    self.idx = idx\n    self.classes = classes or []\n    self.extra_classes = extra_classes or []\n    self.v_content = v_content\n    self.v_if = v_if\n    self.v_for = v_for\n    self.content = content or []\n    self.no_closing = no_closing\n    self.additional_attrs = {}\n    # Using other args as `style` data\n    self.styles = {}\n    for k, v in kwargs.items():\n        if not v:\n            continue\n        if k.startswith(\"attr__\"):\n            self.additional_attrs[k.split(\"attr__\")[-1]] = v\n            continue\n        self.styles[k.replace(\"_\", \"-\")] = v\n</code></pre>"},{"location":"api_reference.html#pyue.Component.to_lines","title":"<code>to_lines(level=0)</code>","text":"<p>Render the component to a list of HTML strings.</p> <pre><code>    This method generates a list of the complete HTML representations of the component parts,\n    including all child components and applying any Jinja2 template directives\n    (v_if, v_for, v_content). The rendering process:\n    1. Processes the component's HTML tag and attributes\n    2. Recursively renders all child components\n    3. Applies Jinja2 template syntax for dynamic content\n    4. Combines everything into a final list of HTML strings\n\n    Returns:\n        List of fully rendered HTML content of the component as strings.\n\n    Raises:\n        ComponentBuildingError: If an error occurs during template strings\n            rendering, such as malformed template syntax or missing variables.\n\n    Example:\n        ```python\n        component = Component(tag=\"h1\", v_content=\"title\")\n        html = component.to_lines()\n        print(html)\n        # ['&lt;h1&gt;', '    {{ title }}', '\n</code></pre> <p>']             ```</p> Source code in <code>pyue/core/component.py</code> <pre><code>def to_lines(self, level: int = 0) -&gt; list[str]:\n    \"\"\"Render the component to a list of HTML strings.\n\n    This method generates a list of the complete HTML representations of the component parts,\n    including all child components and applying any Jinja2 template directives\n    (v_if, v_for, v_content). The rendering process:\n    1. Processes the component's HTML tag and attributes\n    2. Recursively renders all child components\n    3. Applies Jinja2 template syntax for dynamic content\n    4. Combines everything into a final list of HTML strings\n\n    Returns:\n        List of fully rendered HTML content of the component as strings.\n\n    Raises:\n        ComponentBuildingError: If an error occurs during template strings\n            rendering, such as malformed template syntax or missing variables.\n\n    Example:\n        ```python\n        component = Component(tag=\"h1\", v_content=\"title\")\n        html = component.to_lines()\n        print(html)\n        # ['&lt;h1&gt;', '    {{ title }}', '\\n&lt;/h1&gt;']\n        ```\n    \"\"\"\n    try:\n        indent = \" \" * 4 * level\n        inner_indent = \" \" * 4 * (level + 1)\n        result = []\n\n        attrs = []\n        if self.idx:\n            attrs.append(f'id=\"{self.idx}\"')\n        if self.classes or self.extra_classes:\n            classes = self.classes + self.extra_classes\n            attrs.append(f'class=\"{\" \".join(classes)}\"')\n\n        if self.additional_attrs:\n            additional_attrs = []\n            for k, v in self.additional_attrs.items():\n                additional_attrs.append(f'{k}=\"{v}\"')\n            attrs.extend(additional_attrs)\n\n        if self.styles:\n            styles = []\n            for k, v in self.styles.items():\n                styles.append(f\"{k}:{v}\")\n\n            attrs.append(f'style=\"{\";\".join(styles)}\"')\n        attrs = \" \" + \" \".join(attrs) if attrs else \"\"\n\n        result.append(f\"{indent}&lt;{self.tag}{attrs}&gt;\")\n\n        if self.v_content:\n            result.append(f\"{inner_indent}{{{{ {self.v_content} }}}}\")\n\n        for content in self.content:\n            if isinstance(content, str):\n                result.append(inner_indent + content)\n                continue\n            elif isinstance(content, Component):\n                result.extend(content.to_lines(level=level + 1))\n\n        # For handling tags like &lt;hr&gt;\n        if not self.no_closing:\n            result.append(f\"\\n{indent}&lt;/{self.tag}&gt;\")\n\n        if self.v_if:\n            result = (\n                [f\"{indent}{{% if {self.v_if} %}}\"]\n                + result\n                + [f\"{indent}{{% endif %}}\"]\n            )\n        if self.v_for:\n            result = (\n                [f\"{indent}{{% for {self.v_for} %}}\"]\n                + result\n                + [f\"{indent}{{% endfor %}}\"]\n            )\n\n        return result\n    except Exception:\n        raise ComponentBuildingError(traceback.format_exc())\n</code></pre>"},{"location":"api_reference.html#pyue.Component.to_string","title":"<code>to_string(level=0)</code>","text":"<p>Return the component as a single string.</p> Source code in <code>pyue/core/component.py</code> <pre><code>def to_string(self, level: int = 0) -&gt; str:\n    \"\"\"Return the component as a single string.\"\"\"\n    return \"\\n\".join(self.to_lines(level))\n</code></pre>"},{"location":"api_reference.html#stack","title":"Stack","text":"<p>               Bases: <code>Component</code></p> <p>A container that stacks its children vertically or horizontally with consistent spacing. Uses flexbox with flex-col (vertical) or flex-row (horizontal) and configurable gap.</p> Source code in <code>pyue/components/layouts/stack.py</code> <pre><code>class Stack(Component):\n    \"\"\"\n    A container that stacks its children vertically or horizontally with consistent spacing.\n    Uses flexbox with flex-col (vertical) or flex-row (horizontal) and configurable gap.\n    \"\"\"\n\n    def __init__(\n        self,\n        direction: str = \"vertical\",\n        spacing: Union[int, str, None] = None,\n        align: Optional[str] = None,\n        justify: Optional[str] = None,\n        **kwargs,\n    ):\n        \"\"\"\n        Args:\n            direction: \"vertical\" (column) or \"horizontal\" (row).\n            spacing: gap between children (int for Tailwind gap-{n} or custom class).\n            align: Tailwind class for align-items (e.g., \"items-center\").\n            justify: Tailwind class for justify-content (e.g., \"justify-between\").\n\n        Example:\n            ```python\n\n            # Vertical stack with spacing 4\n            Stack(direction=\"vertical\", spacing=4,\n                content=[\n                    H1(\"Title\"),\n                    P(\"Some description\"),\n                    Button(\"Click me\")\n                ]\n            )\n\n            # Horizontal stack with centered alignment\n            Stack(direction=\"horizontal\", spacing=2, align=\"items-center\",\n                content=[\n                    Icon(\"home\"),\n                    Span(\"Home\"),\n                    Span(\"/\"),\n                    Span(\"Products\")\n                ]\n            )\n            ```\n        \"\"\"\n        classes = [\"flex\"]\n        if direction == \"vertical\":\n            classes.append(\"flex-col\")\n        else:\n            classes.append(\"flex-row\")\n        if spacing is not None:\n            if isinstance(spacing, int):\n                classes.append(f\"gap-{spacing}\")\n            else:\n                classes.append(spacing)\n        if align:\n            classes.append(align)\n        if justify:\n            classes.append(justify)\n\n        super().__init__(tag=\"div\", classes=classes, **kwargs)\n</code></pre>"},{"location":"api_reference.html#pyue.components.Stack.__init__","title":"<code>__init__(direction='vertical', spacing=None, align=None, justify=None, **kwargs)</code>","text":"<p>Parameters:</p> Name Type Description Default <code>direction</code> <code>str</code> <p>\"vertical\" (column) or \"horizontal\" (row).</p> <code>'vertical'</code> <code>spacing</code> <code>Union[int, str, None]</code> <p>gap between children (int for Tailwind gap-{n} or custom class).</p> <code>None</code> <code>align</code> <code>Optional[str]</code> <p>Tailwind class for align-items (e.g., \"items-center\").</p> <code>None</code> <code>justify</code> <code>Optional[str]</code> <p>Tailwind class for justify-content (e.g., \"justify-between\").</p> <code>None</code> Example <pre><code># Vertical stack with spacing 4\nStack(direction=\"vertical\", spacing=4,\n    content=[\n        H1(\"Title\"),\n        P(\"Some description\"),\n        Button(\"Click me\")\n    ]\n)\n\n# Horizontal stack with centered alignment\nStack(direction=\"horizontal\", spacing=2, align=\"items-center\",\n    content=[\n        Icon(\"home\"),\n        Span(\"Home\"),\n        Span(\"/\"),\n        Span(\"Products\")\n    ]\n)\n</code></pre> Source code in <code>pyue/components/layouts/stack.py</code> <pre><code>def __init__(\n    self,\n    direction: str = \"vertical\",\n    spacing: Union[int, str, None] = None,\n    align: Optional[str] = None,\n    justify: Optional[str] = None,\n    **kwargs,\n):\n    \"\"\"\n    Args:\n        direction: \"vertical\" (column) or \"horizontal\" (row).\n        spacing: gap between children (int for Tailwind gap-{n} or custom class).\n        align: Tailwind class for align-items (e.g., \"items-center\").\n        justify: Tailwind class for justify-content (e.g., \"justify-between\").\n\n    Example:\n        ```python\n\n        # Vertical stack with spacing 4\n        Stack(direction=\"vertical\", spacing=4,\n            content=[\n                H1(\"Title\"),\n                P(\"Some description\"),\n                Button(\"Click me\")\n            ]\n        )\n\n        # Horizontal stack with centered alignment\n        Stack(direction=\"horizontal\", spacing=2, align=\"items-center\",\n            content=[\n                Icon(\"home\"),\n                Span(\"Home\"),\n                Span(\"/\"),\n                Span(\"Products\")\n            ]\n        )\n        ```\n    \"\"\"\n    classes = [\"flex\"]\n    if direction == \"vertical\":\n        classes.append(\"flex-col\")\n    else:\n        classes.append(\"flex-row\")\n    if spacing is not None:\n        if isinstance(spacing, int):\n            classes.append(f\"gap-{spacing}\")\n        else:\n            classes.append(spacing)\n    if align:\n        classes.append(align)\n    if justify:\n        classes.append(justify)\n\n    super().__init__(tag=\"div\", classes=classes, **kwargs)\n</code></pre>"},{"location":"installation.html","title":"Installation","text":"<p>There are two ways of installation:</p> <ul> <li>via PyPI</li> <li>from source</li> </ul>"},{"location":"installation.html#via-pypi","title":"Via PyPI","text":"<p>You can simply install Pyue via PyPI: <pre><code># Basic (without backends, only for tests)\npip install pyue\n\n# With Flask\npip install pyue[flask]\n\n# With packages for developing\npip install pyue[dev]\n</code></pre></p>"},{"location":"installation.html#from-source","title":"From source","text":"<p>Getting from source is also an option: <pre><code>git clone &lt;repo&gt;\ncd pyue\npip install .\n</code></pre></p> <p>Now it's time to create our first project</p>"},{"location":"quickstart.html","title":"Pyue first project","text":"<p>First of all, we should generate project tree. Use <code>pyue</code> cmd tool for that: <pre><code>pyue i project_name\n</code></pre></p> <p>Now you can see project structure like that: <pre><code>.\n\u251c\u2500\u2500 controllers\n\u251c\u2500\u2500 main.py\n\u251c\u2500\u2500 pages\n\u251c\u2500\u2500 static\n\u2514\u2500\u2500 templates\n</code></pre></p> <ul> <li><code>controllers</code> folder should be used to store API handlers</li> <li><code>main.py</code> file is entrypoint for your web app</li> <li><code>pages</code> folder should be used to store Pyue components</li> <li><code>static</code> folder stores images, libs and other static files</li> <li><code>templates</code> folder stores html/templates, generated by Pyue</li> </ul> <p>If you look in <code>main.py</code> you will see an example like that: <pre><code>from pyue import Pyue, BackendType, Page\nfrom flask import Flask\n\n# Describing page\np = Page(title=\"My Site\")\n\n# Creating Pyue app\napp = Pyue(BackendType.Flask)\n\n# Register page like that:\napp.add_page(page=p, url=\"/\")\n\nf = Flask(__name__)\napp.mount(f)\n\nf.run()\n</code></pre></p> <p>As you can see:</p> <ul> <li><code>Page</code> is used to define web pages</li> <li><code>Pyue</code> is main point for web UI at all</li> <li><code>Pyue.add_page(...)</code> registers your <code>Page</code> components</li> <li><code>Pyue.mount(...)</code> registers itself in backend (ex. Flask)</li> </ul> <p>To start our web app we must run <code>main.py</code>: <pre><code>python main.py\n</code></pre></p>"}]}