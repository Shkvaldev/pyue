# Installation
There are two ways of installation:

- [via PyPI](#via-pypi)
- [from source](#from-source)

## :simple-pypi: Via PyPI
You can simply install Pyue via PyPI:
```bash
# Basic (without backends, only for tests)
pip install pyue

# With Flask
pip install pyue[flask]

# With packages for developing
pip install pyue[dev]
```

## :fontawesome-brands-square-git: From source
Getting from source is also an option:
```bash
git clone <repo>
cd pyue
pip install .
```

Now it's time to [create our first project](quickstart.md)
